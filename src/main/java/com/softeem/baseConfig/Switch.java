package com.softeem.baseConfig;
public final class Switch {
    // 调时模式
    public static boolean DEBUG = false;
    // 内部测试
    public static boolean INTERNAL_TEST = true;
    // 使用牌库
    public static boolean USE_CARD_LIB = false;
    // 使用牌库--扑克
    public static boolean USE_CARD_LIB_POKER = false;
    // 使用相同的色子
    public static boolean USE_SAME_CRAP = false;
    // 开启手机验证码
    public static boolean PHONE_AUTH = true;
    // 开启权限验证
    public static long PLAYER_PRIVILEGE_VERIFY = 0x0000;//  ff 关，00开
}